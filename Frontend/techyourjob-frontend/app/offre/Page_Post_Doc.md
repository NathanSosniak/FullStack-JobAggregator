# [NOM_DE_LA_FEATURE]

# [Type de features] (exemple API, route, ...)

## Description de la features :

(donc ec qu'elle fait concretement genre aller chercher des donnees)

## Technique

### Dependances :

(- Libs / packages utilisés

- Services externes
- Autres features internes
- Modèles / schémas / composants réutilisés)

### Fichier cles :

(fichier utilise ou lies a cette features)
(Tout fichier important pour comprendre ou modifier la feature)

### Flux de donnees

(chemins de donnees donc par exemple API -> stockage -> ...)
(Décrire le cheminement des données)

### 🧠 Logique de fonctionnement

(comment ca marche concretement)

### 🧪 Tests

(test implementer ou reaqlise pour tester la ou les features, si c'est ia on met r², route on met le CI de github, ...)

### 🔐 Sécurité

(roles, permissions, mesure preventive, donnees, ...)

## 🚀 Déploiement / Configuration

(est-ce que c'est deployer ? si oui dans quel conteneur, si ca appartient a celui backend, celui front, ...)

## 📌 Notes complémentaires

(si vous voukez rajouter des elements pour la comprehension des autres dev)
